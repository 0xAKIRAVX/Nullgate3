# راهنمای قدم‌به‌قدم دیپلوی NullGate 3.0 روی Railway 🚄

این راهنما طوری نوشته شده که حتی اگه بار اولت باشه، از صفر تا صد بری. فقط مرحله‌به‌مرحله جلو برو و از هیچ مرحله‌ای نپر.

---

## ۰) چی می‌سازیم؟ (نقشه کلی)

پنل 3.0 از **۴ سرویس** تشکیل شده که کنار هم روی Railway اجرا می‌شن:

| سرویس | چی هست؟ | به زبان آدمیزاد |
|---|---|---|
| **postgres** | دیتابیس | گاوصندوق اطلاعات: کاربرها، کلیدهای Reality، آمار مصرف |
| **redis** | حافظه موقت | جلسه‌های ورود ادمین (که با رفرش لاگ‌اوت نشی) |
| **api** | بک‌اند Go + موتور Xray | مغز پنل: همه‌ی منطق اینجاست، Xray هم همین‌جا اجرا می‌شه |
| **web** | فرانت Next.js | همون صفحات مشکی-طلایی که می‌بینی؛ کاربر فقط با این سرویس حرف می‌زنه |

```
مرورگر/موبایل ──HTTP──▶ web ──داخلی──▶ api ──▶ postgres + redis
                                │
کلاینت‌های v2rayNG ──TCP Proxy──▶ Xray (داخل api)
```

> هزینه: ۴ سرویس یعنی تقریباً ۲ تا ۳ برابر پنل قبلی — همون‌طور که موقع انتخاب گزینه 🅰️ قبول کردی، این یعنی پایداری خیلی بیشتر.

---

## ۱) آماده‌سازی: کد روی GitHub

Railway از گیت‌هاب سرویس می‌سازه. پس اول:

1. محتویات پوشه `nullgate-api` (که توی ZIP تحویلی هست) رو بذار توی یک ریپوی GitHub (پایه یا خصوصی فرقی نمی‌کنه).
2. ساختارش باید این شکلی باشه:

```
nullgate-api/          ← ریشه ریپو
├── Dockerfile         ← مال سرویس api
├── docker-compose.yml ← فقط برای تست محلی، به Railway ربطی نداره
├── go.mod / go.sum
├── cmd/  internal/
└── web/               ← مال سرویس web (Dockerfile خودش داخلشه)
    ├── Dockerfile
    └── src/ ...
```

3. توی Railway از منوی **Account Settings → Connections** گیت‌هابت رو وصل کن.

---

## ۲) ساخت پروژه

1. توی Railway دکمه **New Project** رو بزن.
2. **Empty Project** رو انتخاب کن.
3. چهار بار از دکمه **+ New** استفاده می‌کنیم تا چهار سرویس بسازیم (مراحل ۳ تا ۶).

---

## ۳) سرویس PostgreSQL

1. **+ New → Database → PostgreSQL**.
2. همین! Railway خودش دیتابیس رو بالا میاره و یه متغیر `DATABASE_URL` براش می‌سازه.
3. اسمش رو بذار `postgres` (تنظیمات سرویس → نام).

⚠️ **هیچ چیزی توی دیتابیس دستی نساز.** پنل خودش موقع اولین اجرا، جدول‌ها و کلیدهای Reality رو می‌سازه (مایگریشن خودکار).

---

## ۴) سرویس Redis

1. **+ New → Database → Redis** (یا از Marketplace گزینه Redis).
2. اینم همون. متغیر `REDIS_URL` خودش ساخته می‌شه.

> اگه Redis نیاد بالا، پنل می‌شکنه؟ **نه** — بک‌اند اگه Redis نبینن، جلسه‌ها رو توی حافظه نگه می‌داره. ولی ما همون‌طور که قراره ۴ سرویس رو می‌خوایم، Redis رو هم می‌ذاریم.

---

## ۵) سرویس api (بک‌اند Go + Xray) — مهم‌ترین بخش

### ۵-۱) ساخت سرویس

1. **+ New → GitHub Repo** → ریپوی خودت رو انتخاب کن.
2. توی تنظیمات سرویس (Settings):
   - **Root Directory** = `/` (همون ریشه ریپو)
   - Railway خودش `Dockerfile` ریشه رو پیدا می‌کنه و ایمیج رو می‌سازه.

### ۵-۲) متغیرها (Variables)

این جدول رو دقیقاً کپی کن:

| متغیر | مقدار | توضیح ساده |
|---|---|---|
| `PORT` | `8080` | پورت داخلی API |
| `DATABASE_URL` | `${{postgres.DATABASE_URL}}` | آدرس دیتابیس — با {{ }} آدرس رو از سرویس Postgres می‌کشه |
| `REDIS_URL` | `${{redis.REDIS_URL}}` | آدرس Redis — همون ترفند بالا |
| `SECRET` | یه رشته طولانی تصادفی | برای امضای لینک اشتراک (مثلاً خروجی `openssl rand -hex 32`) |
| `REALITY_SNI` | `www.samsung.com` | دامنه پوشاننده. پیش‌فرض همینه؛ **مایکروسافت نذار** (post-quantum مایکروسافت با REALITY تضاد داره — همون مشکلی که قبلاً تست کردیم) |
| `COOKIE_INSECURE` | خالی بذار / حذفش کن | فقط برای تست محلی http هست؛ روی Railway https داریم پس نباشه |

بعد از ساختن TCP Proxy (قدم ۵-۳) دو متغیر دیگه هم اضافه می‌کنیم.

### ۵-۳) TCP Proxy برای Reality — قدم‌به‌قدم

کلاینت‌های v2rayNG برای وصل شدن به Reality یه **آدرس IP:پورت عمومی** لازم دارن. توی Railway، کانتینرها IP عمومی ندارن؛ به‌جاش Railway یه «TCP Proxy» می‌ده که پورت عمومی رو به یه پورت داخلی وصل می‌کنه.

1. سرویس `api` → تب **Settings → Networking → TCP Proxy** (یا Generate TCP Proxy).
2. **TCP Application Port** رو بذار `9000`.
   - یعنی: «هرکی به پورت عمومی وصل شد، ببرش به پورت ۹۰۰۰ داخل کانتینر api» — و پورت ۹۰۰۰ دقیقاً جاییه که inbound منطقی Reality گوش می‌ده.
3. Railway یه آدرس می‌ده، چیزی شبیه: `xxxx.proxy.rlwy.net` و یه پورت مثل `45833`.
4. حالا دو متغیر به سرویس `api` اضافه کن:

| متغیر | مقدار | توضیح |
|---|---|---|
| `TCP_HOST` | `xxxx.proxy.rlwy.net` | همون دامنه‌ای که Railway داد |
| `TCP_PORT` | `45833` | همون پورت عمومی |
| `REALITY_NET` | `grpc` *(اختیاری)* | اگه gRPC می‌خوای: Reality داخلی gun/h2 می‌شه (از روی همین TCP Proxy — پنل لینک‌ها رو خودکار عوض می‌کنه). حذفش کنی برمی‌گرده به tcp |

5. یک بار **Deploy / Redeploy** بزن تا سرویس با این متغیرها بالا بیاد. از این به بعد لینک‌های Reality که پنل می‌سازه خودکار همین آدرس رو دارن.

> 💡 **چرا gRPC سفارشی روی Railway راه نداره؟** Railway به هر سرویس فقط **یک TCP Proxy** می‌ده و دروازه‌ی ۴۴۳ هم ترافیک رو با HTTP/1.1 به سرویس می‌رسونه (gRPC به HTTP/2 نیاز داره). راه درست gRPC روی Railway همون `REALITY_NET=grpc` هست که از روی TCP Proxy موجود رد می‌شه.

### ۵-۴) دامنه داخلی (برای سرویس web)

توی تب **Settings → Networking** بخش Private Networking، دامنه داخلی سرویس api رو بردار — چیزی شبیه:
```
api.railway.internal
```
چند خط پایین‌تر بهش نیاز داریم. پورت داخلی هم `8080` هست (همون `PORT`).

---

## ۶) سرویس web (فرانت Next.js)

1. **+ New → GitHub Repo** → همون ریپو، ولی این بار:
   - **Root Directory** = `web`
   - Railway `Dockerfile` داخل پوشه `web` رو پیدا می‌کنه.
2. متغیرها:

| متغیر | مقدار | توضیح |
|---|---|---|
| `API_INTERNAL_URL` | `http://api.railway.internal:8080` | آدرس داخلی سرویس api از قدم ۵-۴. فرانت درخواست‌های `/api/*` و `/sub/*` رو سمت سرور به همین آدرس می‌فرسته (کوکی‌ها همون دامنه می‌مونن) |
| `PORT` | `3000` | پورت داخلی Next.js |

3. تب **Settings → Networking → Generate Domain** → پورت پیشنهادی `3000` → یه دامنه مثل `xxxx.up.railway.app` می‌گیری. **این آدرس پنلته** — توکمرکش کن.

---

## ۷) اولین ورود 🎉

1. دامنه سرویس `web` رو توی مرورگر باز کن.
2. چون هنوز ادمینی وجود نداره، پنل مستقیم می‌بردت به **صفحه راه‌اندازی اولیه (Setup)**.
3. یه نام کاربری و رمز قوی بساز — این با bcrypt واقعی توی PostgreSQL ذخیره می‌شه.
4. لاگین کن! از اینجا به بعد: کاربر بساز، لینک بگیر، QR بزن، روی گوشی import کن.

> اگه صفحه setup نیومد و مستقیم لاگین خواست، یعنی setup قبلاً انجام شده — رمز فراموش‌شده رو فعلاً باید از دیتابیس ریست کنی (جدول `admins`).

---

## ۸) ⚠️ پورت ۹۰۰۰ فقط مال Reality است (مهم‌ترین نکته‌ی کل راهنما)

توی تست‌های فاز ۴ این حالت رو دیدیم:

> **اگه پورت TCP Proxy (پیش‌فرض ۹۰۰۰) قبلاً اشغال باشه، Xray استارت نمی‌شه و پنل درست وضعیتش رو «خاموش» نشون می‌ده.**

چی می‌شه؟ توی پنل بخش Inbounds می‌تونی inbound سفارشی بسازی (XHTTP / WS / ...). اگه اشتباهی یه inbound روی پورت **۹۰۰۰** بسازی، دیگه Reality نمی‌تونه روی پورت خودش گوش بده → Xray کل پیکربندی رو رد می‌کنه → کانفیگ‌ها همه خاموش می‌شن.

**راه‌حل (به ترتیب):**
1. برو Inbounds → inbound سفارشی که پورت ۹۰۰۰ رو گرفته پاک کن (یا پورتش رو عوض کن).
2. اگه واقعاً می‌خوای Reality روی پورت دیگه‌ای باشه: متغیر `TCP_APP_PORT` سرویس api رو عوض کن **و** TCP Application Port توی تنظیمات Railway رو هم با همون عدد هماهنگ کن.
3. بعد از هر تغییر، دکمه **Restart Xray** توی تنظیمات پنل (یا Deploy توی Railway) رو بزن.

پورت `10085` هم مال API داخلی آمار Xray است — اون رو هم اشغال نکن.

---

## ۹) آپدیت و Redeploy — چرا دیگه هیچی پاک نمی‌شه 😌

توی پنل ۲.x هر Redeploy، فایل `state.json` رو ریست می‌کرد و کلیدهای Reality از دست می‌رفت (همون باگی که گوشیت رو خاموش کرده بود).

توی ۳.۰ **کلیدهای Reality و همه‌چیز توی PostgreSQL است** — و PostgreSQL حجمش مونگاست:

- **Redeploy** (همون کد، دوباره بیلد): هیچی از دست نمی‌ره. کانفیگ‌ها و کاربرها سرجاشونن.
- **آپدیت کد پنل** (پول جدید از گیت‌هاب): باز هم دیتابیس دست‌نخورده می‌مونه؛ مایگریشن‌های جدید خودکار اعمال می‌شن.
- فقط اگه خودت سرویس PostgreSQL رو Delete کنی دیتا می‌پره — پس نکن 😄

---

## ۱۰) جدول رفع اشکال

| علامت | احتمالاً چیه | چیکار کن |
|---|---|---|
| صفحه web باز نمی‌شه / 502 | سرویس web هنوز بیلد نشده یا crash کرده | تب Deployments → لاگ بیلد رو ببین |
| لاگین می‌زنی، دوباره صفحه لاگین میاد | کوکی Secure روی http فعال بوده | مطمئن شو `COOKIE_INSECURE` روی Railway **ست نشده** و دامنه https است |
| پنل می‌گه Xray «خاموش» | پورت ۹۰۰۰ اشغاله یا کلیدها آماده نیستن | بخش ۸ همین راهنما + لاگ‌های سرویس api |
| کانفیگ روی گوشی Timeout می‌شه | TCP Proxy ساخته نشده یا `TCP_HOST/TCP_PORT` غلطه | قدم ۵-۳ رو دوباره چک کن؛ لینک جدید بساز و import کن |
| لینک آدرس localhost داره | `TCP_HOST` هنوز ست نشده | متغیرها رو ست کن و Redeploy بزن |
| آمار صفر می‌مونه | WebSocket وصل نیست | صفحه رو رفرش کن؛ بج «زنده» باید روشن باشه |
| « setup را دیدم ولی بعدش ۴۰۴» | `API_INTERNAL_URL` غلطه | قدم ۶ — باید `http://api.railway.internal:8080` باشه |

---

## ۱۱) چک‌لیست نهایی قبل از خداحافظی ✅

- [ ] ۴ سرویس سبزن (postgres / redis / api / web)
- [ ] متغیرهای api: `DATABASE_URL` و `REDIS_URL` با `${{...}}` به دیتابیس‌ها وصله
- [ ] TCP Proxy روی پورت ۹۰۰۰ ساخته شده و `TCP_HOST` / `TCP_PORT` ست شدن
- [ ] `SECRET` یه مقدار تصادفی طولانیه
- [ ] web دامنه داره و `API_INTERNAL_URL` رو داره
- [ ] Setup اولیه انجام شده و رمز ادمین جایی امن ذخیره شده
- [ ] یه کاربر تستی ساختی و روی گوشی وصل شد 🎉
