# راهنمای قدم‌به‌قدم دیپلوی NullGate 3.0 روی Railway 🚄

این راهنما طوری نوشته شده که حتی اگه بار اولت باشه، از صفر تا صد بری. فقط مرحله‌به‌مرحله جلو برو و از هیچ مرحله‌ای نپر.

> **خبر خوب: از این به بعد هیچ متغیری را دستی تایپ نمی‌کنی.** 😎
> پنل ۳.۰ الان تک‌سرویسی است (UI داخل باینری Go جاسازی شده) و همه‌ی اتصال‌ها با **کلیک** و تزریق خودکار Railway انجام می‌شود:
>
> | چی قبلاً دستی بود | الان چی شده |
> |---|---|
> | `DATABASE_URL` = `${{postgres.DATABASE_URL}}` | دکمه‌ی **Connect** در UI Railway — متغیر خودش تزریق می‌شود |
> | `SECRET` = خروجی `openssl rand -hex 32` | کلاً حذف شد — این متغیر اصلاً استفاده نمی‌شد (توکن‌های ساب‌اسکریپشن تصادفی و در دیتابیس ذخیره می‌شوند) |
> | `TCP_HOST` / `TCP_PORT` کپی‌شون دستی | بعد از Generate TCP Proxy، پنل `RAILWAY_TCP_PROXY_DOMAIN` / `RAILWAY_TCP_PROXY_PORT` را خودش می‌خواند |
> | `PORT` | Railway خودش تزریق می‌کند |
> | `REALITY_SNI` و بقیه | پیش‌فرض دار‌ند — فقط اگه خواستی عوض کن |

---

## ۰) چی می‌سازیم؟ (نقشه کلی)

پنل 3.0 روی Railway از **۲ تا ۳ سرویس** تشکیل شده (دیگه سرویس جدا برای فرانت نداریم — UI داخل باینری جاسازی شده):

| سرویس | چی هست؟ | به زبان آدمیزاد |
|---|---|---|
| **postgres** | دیتابیس | گاوصندوق اطلاعات: کاربرها، کلیدهای Reality، آمار مصرف |
| **redis** (اختیاری) | حافظه موقت | جلسه‌های ورود ادمین — اگه نباشه، حافظه‌ی خود پروسه استفاده می‌شه |
| **api** | Go + Xray + UI جاسازی‌شده | مغز پنل و همون موقع صفحات پنل را هم سرو می‌کند |

```
مرورگر/موبایل ──HTTPS──▶ api (UI + /api + /sub) ──▶ postgres (+ redis)
کلاینت‌های v2rayNG ──TCP Proxy──▶ Xray (داخل api)
```

> سرویس Redis واقعاً اختیاری است — پنل بدونش هم کامل کار می‌کند. اگه پلن رایگان کم میاری، بگذارش برای بعد.

---

## ۱) آماده‌سازی: کد روی GitHub

Railway از گیت‌هاب سرویس می‌سازه. پس اول:

1. این ریپو را fork/کپی کن به حساب خودت (پایه یا خصوصی فرقی نمی‌کنه).
2. توی Railway از منوی **Account Settings → Connections** گیت‌هابت رو وصل کن.

---

## ۲) ساخت پروژه

1. توی Railway دکمه **New Project** رو بزن.
2. **Empty Project** رو انتخاب کن.

---

## ۳) سرویس PostgreSQL

1. **+ New → Database → PostgreSQL**.
2. همین! اسمش رو بذار `postgres` (تنظیمات سرویس → نام).

⚠️ **هیچ چیزی توی دیتابیس دستی نساز.** پنل خودش موقع اولین اجرا، جدول‌ها و کلیدهای Reality رو می‌سازه (مایگریشن خودکار).

---

## ۴) سرویس api — بدون حتی یک متغیر دستی

### ۴-۱) ساخت سرویس

1. **+ New → GitHub Repo** → ریپوی خودت رو انتخاب کن.
2. توی تنظیمات سرویس (Settings):
   - **Root Directory** = `/` (همون ریشه ریپو)
   - Railway خودش `Dockerfile` ریشه رو پیدا می‌کنه و ایمیج رو می‌سازه (UI هم داخل همون بیلد استاتیک اکسپورت و جاسازی می‌شه).

### ۴-۲) وصل کردن دیتابیس — فقط کلیک، بدون تایپ ✨

1. روی کارت سرویس **postgres** کلیک کن → **Connect** (یا توی Railway جدید: منوی سرویس → Connect to service).
2. سرویس **api** رو از لیست انتخاب کن.
3. تمام — Railway متغیرهای `DATABASE_URL` و `PG*` رو روی سرویس api خودش می‌گذارد. تو هیچی ننوشتی!

> اگه Redis هم گذاشتی: همین کار را برای **redis** هم کن تا `REDIS_URL` خودش بیاید.

### ۴-۳) TCP Proxy برای Reality — کلیک، بدون کپی‌کردن ✨

کلاینت‌های v2rayNG برای وصل شدن به Reality یه **آدرس IP:پورت عمومی** لازم دارن. Railway با TCP Proxy این را می‌دهد:

1. سرویس `api` → تب **Settings → Networking → TCP Proxy** (یا Generate TCP Proxy).
2. **TCP Application Port** رو بذار `9000` — دقیقاً همون پورتی که Reality داخل کانتینر گوش می‌دهد.
3. Railway یه آدرس عمومی می‌سازد (مثل `xxxx.proxy.rlwy.net:45833`) و متغیرهای `RAILWAY_TCP_PROXY_DOMAIN`، `RAILWAY_TCP_PROXY_PORT` و `RAILWAY_TCP_APPLICATION_PORT` را خودکار روی سرویس تزریق می‌کند.
4. **پنل این‌ها را خودش می‌خواند** — از این به بعد لینک‌های Reality که پنل می‌سازه خودکار همین آدرس عمومی را دارند. لازم نیست `TCP_HOST`/`TCP_PORT` را جایی کپی کنی.

> 💡 اگه دوست داری آدرس دیگه‌ای override کنی (مثلاً دامنه شخصی که به همین پروکسی اشاره می‌کند)، اون موقع می‌تونی `TCP_HOST` / `TCP_PORT` را دستی بگذاری — ولی برای دیپلوی معمولی لازم نیست.

### ۴-۴) دامنه پنل

تب **Settings → Networking → Generate Domain** → پورت `8080` → یه دامنه مثل `xxxx.up.railway.app` می‌گیری. **این آدرس پنلته** — توکنش رو کنار.

---

## ۵) اولین ورود 🎉

1. دامنه سرویس `api` رو توی مرورگر باز کن.
2. چون هنوز ادمینی وجود نداره، پنل مستقیم می‌بردت به **صفحه راه‌اندازی اولیه (Setup)**.
3. یه نام کاربری و رمز قوی بساز — این با bcrypt واقعی توی PostgreSQL ذخیره می‌شه.
4. لاگین کن! از اینجا به بعد: کاربر بساز، لینک بگیر، QR بزن، روی گوشی import کن.

> اگه صفحه setup نیومد و مستقیم لاگین خواست، یعنی setup قبلاً انجام شده — رمز فراموش‌شده رو فعلاً باید از دیتابیس ریست کنی (جدول `admins`).

---

## ۶) ⚠️ پورت ۹۰۰۰ فقط مال Reality است (مهم‌ترین نکته‌ی کل راهنما)

توی تست‌های فاز ۴ این حالت رو دیدیم:

> **اگه پورت TCP Proxy (پیش‌فرض ۹۰۰۰) قبلاً اشغال باشه، Xray استارت نمی‌شه و پنل درست وضعیتش رو «خاموش» نشون می‌ده.**

چی می‌شه؟ توی پنل بخش Inbounds می‌تونی inbound سفارشی بسازی (XHTTP / WS / ...). اگه اشتباهی یه inbound روی پورت **۹۰۰۰** بسازی، دیگه Reality نمی‌تونه روی پورت خودش گوش بده → Xray کل پیکربندی رو رد می‌کنه → کانفیگ‌ها همه خاموش می‌شن.

**راه‌حل (به ترتیب):**
1. برو Inbounds → inbound سفارشی که پورت ۹۰۰۰ رو گرفته پاک کن (یا پورتش رو عوض کن).
2. اگه واقعاً می‌خوای Reality روی پورت دیگه‌ای باشه: متغیر `TCP_APP_PORT` سرویس api رو عوض کن **و** TCP Application Port توی تنظیمات Railway رو هم با همون عدد هماهنگ کن.
3. بعد از هر تغییر، دکمه **Restart Xray** توی تنظیمات پنل (یا Deploy توی Railway) رو بزن.

پورت `10085` هم مال API داخلی آمار Xray است — اون رو هم اشغال نکن.

---

## ۷) گزینه‌های اختیاری (فقط اگه خواستی)

هیچ‌کدوم لازم نیست — پنل با پیش‌فرض‌ها کامل کار می‌کند:

| متغیر | پیش‌فرض | کی عوضش کنی؟ |
|---|---|---|
| `REALITY_NET` | `tcp` | اگه ترنسپورت gRPC (gun) می‌خوای → `grpc` بگذار (از روی همین TCP Proxy رد می‌شه؛ لینک‌ها خودکار gun می‌شوند) |
| `REALITY_SNI` | `www.samsung.com` | اگه SNI پوشاننده دیگه‌ای می‌خوای — **مایکروسافت نذار** (post-quantum مایکروسافت با REALITY تضاد داره) |
| `REALITY_GRPC_SERVICE` | `nullgate` | اسم سرویس gRPC وقتی `REALITY_NET=grpc` است |
| `SESSION_HOURS` | `24` | عمر نشست ادمین |
| `COOKIE_INSECURE` | (خالی) | فقط برای تست محلی http — روی Railway ستش نکن |

جدول کامل متغیرها داخل [README](README.md) است.

> 💡 **چرا gRPC سفارشی روی Railway راه نداره؟** Railway به هر سرویس فقط **یک TCP Proxy** می‌ده و دروازه‌ی ۴۴۳ هم ترافیک رو با HTTP/1.1 به سرویس می‌رسونه (gRPC به HTTP/2 نیاز داره). راه درست gRPC روی Railway همون `REALITY_NET=grpc` هست که از روی TCP Proxy موجود رد می‌شه.

---

## ۸) آپدیت و Redeploy — چرا دیگه هیچی پاک نمی‌شه 😌

توی پنل ۲.x هر Redeploy، فایل `state.json` رو ریست می‌کرد و کلیدهای Reality از دست می‌رفت (همون باگی که گوشیت رو خاموش کرده بود).

توی ۳.۰ **کلیدهای Reality و همه‌چیز توی PostgreSQL است** — و PostgreSQL حجمش مونگاست:

- **Redeploy** (همون کد، دوباره بیلد): هیچی از دست نمی‌ره. کانفیگ‌ها و کاربرها سرجاشونن.
- **آپدیت کد پنل** (پول جدید از گیت‌هاب): باز هم دیتابیس دست‌نخورده می‌مونه؛ مایگریشن‌های جدید خودکار اعمال می‌شن.
- فقط اگه خودت سرویس PostgreSQL رو Delete کنی دیتا می‌پره — پس نکن 😄

---

## ۹) جدول رفع اشکال

| علامت | احتمالاً چیه | چیکار کن |
|---|---|---|
| صفحه پنل باز نمی‌شه / 502 | سرویس api هنوز بیلد نشده یا crash کرده | تب Deployments → لاگ بیلد رو ببین؛ اگه «DATABASE_URL is not set» بود، قدم ۴-۲ رو جا انداختی — postgres را Connect کن |
| لاگین می‌زنی، دوباره صفحه لاگین میاد | کوکی Secure روی http فعال بوده | مطمئن شو `COOKIE_INSECURE` روی Railway **ست نشده** و دامنه https است |
| پنل می‌گه Xray «خاموش» | پورت ۹۰۰۰ اشغاله یا کلیدها آماده نیستن | بخش ۶ همین راهنما + لاگ‌های سرویس api |
| کانفیگ روی گوشی Timeout می‌شه | TCP Proxy ساخته نشده یا به پورت غیری می‌ره | قدم ۴-۳ رو چک کن (پورت مقصد = 9000)؛ لینک جدید بساز و import کن |
| لینک آدرس localhost داره | TCP Proxy هنوز ساخته نشده | Generate TCP Proxy بزن — پنل بعد از restart آدرس را خودکار برمی‌دارد |
| آمار صفر می‌مونه | WebSocket وصل نیست | صفحه رو رفرش کن؛ بج «زنده» باید روشن باشه |

---

## ۱۰) چک‌لیست نهایی قبل از خداحافظی ✅

- [ ] سرویس‌های `postgres` و `api` سبزن (redis اختیاری)
- [ ] postgres با دکمه Connect به api وصل شده (متغیر `DATABASE_URL` خودکار آمده)
- [ ] TCP Proxy روی پورت ۹۰۰۰ ساخته شده (آدرس عمومی خودکار خوانده می‌شود)
- [ ] دامنه https روی پورت 8080 ساخته شده
- [ ] Setup اولیه انجام شده و رمز ادمین جایی امن ذخیره شده
- [ ] یه کاربر تستی ساختی و روی گوشی وصل شد 🎉
